# Paste Figma Variables JSON

Please paste the JSON output from the Figma console (the one you provided earlier) into:

`figma-variables.json`

Then run:

```bash
pnpm run process-console
```

This will generate the actual token files from your Figma data.
